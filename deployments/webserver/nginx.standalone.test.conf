# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/appstore/webserver.pid;
error_log /var/log/sdkwork/appstore/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    include snippets/adaptive-web.maps.conf;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 0.0.0.0:18090;
    }
    server {
        listen 80;
        server_name appstore-test.sdkwork.com appstore-test.birdcoder.com appstore-test.dtupay.com appstore-test.sdkwork.cn appstore-test.birdcoder.cn appstore-test.dtupay.cn appstore-test.skubc.com appstore-test.skubc.cn appstore-test.zowalk.com appstore-test.zowalk.cn appstore-test.offer86.com appstore-test.offer86.cn appstore-test.86offer.com appstore-test.86offer.cn;
        include snippets/gateway-locations.nonproduction.conf;
    }
}
