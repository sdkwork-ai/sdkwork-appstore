# @sdkwork/sdkwork-appstore-harmony-mobile-app-detail

App Detail capability package for the SDKWork App Store HarmonyOS
mobile root.

Owns route identities: `app.store.app-detail.detail` (`/app/:id`).

Receives the App Store app SDK clients by injection; never constructs a client.
