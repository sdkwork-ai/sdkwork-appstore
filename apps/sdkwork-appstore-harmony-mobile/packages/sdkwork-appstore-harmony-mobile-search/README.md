# @sdkwork/sdkwork-appstore-harmony-mobile-search

Search capability package for the SDKWork App Store HarmonyOS
mobile root.

Owns route identities: `app.store.search.index` (`/search`).

Receives the App Store app SDK clients by injection; never constructs a client.
