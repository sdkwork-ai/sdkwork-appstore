# @sdkwork/sdkwork-appstore-harmony-mobile-discover

Discover capability package for the SDKWork App Store HarmonyOS
mobile root.

Owns route identities: `app.store.discover.index` (`/`).

Receives the App Store app SDK clients by injection; never constructs a client.
