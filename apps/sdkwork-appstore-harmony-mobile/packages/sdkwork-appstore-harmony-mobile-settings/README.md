# @sdkwork/sdkwork-appstore-harmony-mobile-settings

Settings capability package for the SDKWork App Store HarmonyOS
mobile root.

Owns route identities: `console.system.settings.index` (`/console/settings`).

Receives the App Store app SDK clients by injection; never constructs a client.
