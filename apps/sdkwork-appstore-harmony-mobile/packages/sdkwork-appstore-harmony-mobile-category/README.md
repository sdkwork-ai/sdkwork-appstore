# @sdkwork/sdkwork-appstore-harmony-mobile-category

Category capability package for the SDKWork App Store HarmonyOS
mobile root.

Owns route identities: `app.store.category.detail` (`/category/:id`).

Receives the App Store app SDK clients by injection; never constructs a client.
