# config/host/

HarmonyOS host and signing metadata templates for `sdkwork-appstore-harmony-mobile`.

These files are secret-free. Signing certificates, private keys, and profile
passwords never belong in source control.
