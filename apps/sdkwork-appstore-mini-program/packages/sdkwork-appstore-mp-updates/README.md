# @sdkwork/appstore-mp-updates

Updates capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.updates.index` (`/updates`).

Receives the generated app SDK client by injection; never constructs one.
