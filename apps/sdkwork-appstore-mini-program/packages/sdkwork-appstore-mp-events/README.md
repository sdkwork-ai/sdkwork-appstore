# @sdkwork/appstore-mp-events

Events capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.events.detail` (`/events/:id`).

Receives the generated app SDK client by injection; never constructs one.
