/** en-US copy fragment for the user-store capability. */
export const userStoreMessages = {
  "title": "Personal store",
  "share": "Share",
  "public": "Public"
} as const;

export type UserStoreMessages = typeof userStoreMessages;
