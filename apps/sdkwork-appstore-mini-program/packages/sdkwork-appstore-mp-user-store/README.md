# @sdkwork/appstore-mp-user-store

User Store capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.user-store.index` (`/user-store`), `app.store.user-store.public` (`/store/:shareToken`).

Receives the generated app SDK client by injection; never constructs one.
