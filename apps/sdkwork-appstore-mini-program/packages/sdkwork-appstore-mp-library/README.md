# @sdkwork/appstore-mp-library

Library capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.library.index` (`/library`).

Receives the generated app SDK client by injection; never constructs one.
