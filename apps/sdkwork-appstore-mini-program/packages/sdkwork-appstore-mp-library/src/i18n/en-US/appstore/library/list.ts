/** en-US copy fragment for the library capability. */
export const libraryMessages = {
  "title": "My library",
  "updates": "Updates",
  "wishlist": "Wishlist"
} as const;

export type LibraryMessages = typeof libraryMessages;
