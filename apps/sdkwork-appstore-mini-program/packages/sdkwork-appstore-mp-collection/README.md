# @sdkwork/appstore-mp-collection

Collection capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.collection.detail` (`/collection/:id`).

Receives the generated app SDK client by injection; never constructs one.
