# @sdkwork/appstore-mp-apps

Apps capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.apps.index` (`/apps`).

Receives the generated app SDK client by injection; never constructs one.
