/** en-US copy fragment for the search capability. */
export const searchMessages = {
  "title": "Search",
  "placeholder": "Search apps and capabilities",
  "results": "Results"
} as const;

export type SearchMessages = typeof searchMessages;
