# @sdkwork/appstore-mp-search

Search capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.search.index` (`/search`).

Receives the generated app SDK client by injection; never constructs one.
