# @sdkwork/appstore-mp-settings

Settings capability package for the SDKWork App Store mini program.

Owns route identities: `console.system.settings.index` (`/console/settings`).

Receives the generated app SDK client by injection; never constructs one.
