# @sdkwork/appstore-mp-wishlist

Wishlist capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.wishlist.index` (`/wishlist`).

Receives the generated app SDK client by injection; never constructs one.
