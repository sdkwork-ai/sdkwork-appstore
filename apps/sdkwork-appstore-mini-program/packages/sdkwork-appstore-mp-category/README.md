# @sdkwork/appstore-mp-category

Category capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.category.detail` (`/category/:id`).

Receives the generated app SDK client by injection; never constructs one.
