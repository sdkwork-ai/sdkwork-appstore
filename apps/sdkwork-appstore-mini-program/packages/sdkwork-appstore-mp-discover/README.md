# @sdkwork/appstore-mp-discover

Discover capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.discover.index` (`/`).

Receives the generated app SDK client by injection; never constructs one.
