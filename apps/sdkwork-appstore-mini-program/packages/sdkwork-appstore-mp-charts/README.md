# @sdkwork/appstore-mp-charts

Charts capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.charts.index` (`/charts`).

Receives the generated app SDK client by injection; never constructs one.
