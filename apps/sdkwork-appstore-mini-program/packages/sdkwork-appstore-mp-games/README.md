# @sdkwork/appstore-mp-games

Games capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.games.index` (`/games`).

Receives the generated app SDK client by injection; never constructs one.
