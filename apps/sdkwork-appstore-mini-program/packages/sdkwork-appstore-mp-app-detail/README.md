# @sdkwork/appstore-mp-app-detail

App Detail capability package for the SDKWork App Store mini program.

Owns route identities: `app.store.app-detail.detail` (`/app/:id`).

Receives the generated app SDK client by injection; never constructs one.
