/** en-US copy fragment for the publisher capability. */
export const publisherMessages = {
  "title": "Publisher console",
  "apps": "Apps",
  "settings": "Settings"
} as const;

export type PublisherMessages = typeof publisherMessages;
