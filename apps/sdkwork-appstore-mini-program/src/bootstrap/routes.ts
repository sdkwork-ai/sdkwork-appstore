import { APPSTORE_MP_ROUTE_TABLE } from "@sdkwork/appstore-mp-core";

/** Route identity surface consumed by `src/app.json` page registration. */
export function createRoutes() {
  return APPSTORE_MP_ROUTE_TABLE;
}
