# sdkwork_appstore_flutter_mobile_events

Events capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.events.detail` (`/events/:id`).

Receives the App Store app SDK clients by injection; never constructs a client.
