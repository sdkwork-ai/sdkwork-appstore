# sdkwork_appstore_flutter_mobile_updates

Updates capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.updates.index` (`/updates`).

Receives the App Store app SDK clients by injection; never constructs a client.
