# sdkwork_appstore_flutter_mobile_wishlist

Wishlist capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.wishlist.index` (`/wishlist`).

Receives the App Store app SDK clients by injection; never constructs a client.
