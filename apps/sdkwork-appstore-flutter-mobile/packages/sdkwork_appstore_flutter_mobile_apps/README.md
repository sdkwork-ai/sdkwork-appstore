# sdkwork_appstore_flutter_mobile_apps

Apps capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.apps.index` (`/apps`).

Receives the App Store app SDK clients by injection; never constructs a client.
