# sdkwork_appstore_flutter_mobile_collection

Collection capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.collection.detail` (`/collection/:id`).

Receives the App Store app SDK clients by injection; never constructs a client.
