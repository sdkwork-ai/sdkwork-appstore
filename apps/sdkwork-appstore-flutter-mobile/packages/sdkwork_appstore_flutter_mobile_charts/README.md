# sdkwork_appstore_flutter_mobile_charts

Charts capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.charts.index` (`/charts`).

Receives the App Store app SDK clients by injection; never constructs a client.
