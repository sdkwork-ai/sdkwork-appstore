# sdkwork_appstore_flutter_mobile_library

Library capability package for the SDKWork App Store Flutter
mobile root.

Owns route identities: `app.store.library.index` (`/library`).

Receives the App Store app SDK clients by injection; never constructs a client.
